#!/bin/sh

/usr/sbin/logrotate /etc/logrotate.d/vault-audit-rotate
EXITVALUE=$?
if [ $EXITVALUE !=0 ]; then
    /usr/bin/logger -t vault-audit-rotate "ROTATE exited abnormally with [$EXITVALUE]"
fi
exit 0

